# Base URL of the deployed backend. Unset locally — api.js falls back to
# http://127.0.0.1:8000, matching the default backend dev server.
VITE_API_URL=

# Only needed if the backend is running with an API key configured
# (see backend/app/auth.py) — leave blank for local dev.
VITE_API_KEY=
